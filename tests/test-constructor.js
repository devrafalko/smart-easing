/*

The required smart-easing library returns the [Function] constructor
  that cannot be called without the new keyword and throws an error

When the new [Animation] instance is created
  it has the [Function] 'first', 'last', 'previous', 'next', 'go', 'chain', 'all', 'reverse', 'boomerang' and 'repeat' chaining methods defined
  it has the [Function] 'pause', 'resume' and 'reset' control methods defined
  it has 'beforeAll', 'afterAll', 'beforeEach', 'afterEach', 'afterDelay', 'render', 'onPause' and 'onResume' events getters and setters accessible
  it has 'state', 'time', 'queue', 'modes', 'defaults' and 'motions' getters accessible
  
The [Animation] instance should not
  have any other than public methods, properties, getters and setters
  let to define any new methods, properties, getters and setters
  let to delete any existing methods, properties, getters and setters
  let to modify any existing methods, properties, getters and setters
*/
